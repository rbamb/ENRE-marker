{
  "name": "enre-marker",
  "displayName": "ENRE-marker",
  "description": "ENRE-marker is a tool for manually label truth entities and relations analysed by well-known tools.",
  "version": "0.0.1",
  "publisher": "ENRE",
  "engines": {
    "vscode": "^1.61.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:enre-marker.management"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "enre-marker.management",
        "title": "Management Center",
        "category": "ENRE-marker"
      },
      {
        "command": "enre-marker.test",
        "title": "Show color in editor view",
        "category": "ENRE-marker"
      }
    ]
  },
  "scripts": {
    "compile:dev-webview": "cd src/webview && npm run compile:dev",
    "compile:dev": "concurrently \"cd src/extension && npm run compile:dev-w\" \"cd src/webview && npm run compile:prod\" \"npm run start:mock\"",
    "compile:dev-no-mock": "concurrently \"cd src/extension && npm run compile:dev-w\" \"cd src/webview && npm run compile:prod\"",
    "compile:ext-only": "cd src/extension && npm run compile:dev-w",
    "compile:prod": "concurrently \"cd src/extension && npm run compile:prod\" \"cd src/webview && npm run compile:prod\"",
    "start:mock": "cd src/mock && npm start",
    "vscode:prepublish": "npm run package"
  },
  "devDependencies": {
    "concurrently": "^6.3.0"
  }
}